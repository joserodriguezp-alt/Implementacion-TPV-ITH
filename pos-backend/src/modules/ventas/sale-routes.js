// Rutas del módulo ventas
import { Router } from 'express'

const router = Router()

// TODO: implementar endpoints de ventas (TASK-026 a TASK-031)
// POST   /api/ventas
// GET    /api/ventas
// GET    /api/ventas/:id
// POST   /api/ventas/:id/cancelar    (solo administrador)
// POST   /api/ventas/:id/devolucion  (solo administrador)

export default router
