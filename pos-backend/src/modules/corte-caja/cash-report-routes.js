// Rutas del módulo corte de caja
import { Router } from 'express'

const router = Router()

// TODO: implementar endpoints de corte de caja (TASK-035 a TASK-039)
// POST   /api/corte-caja              (solo administrador)
// PUT    /api/corte-caja/:id/cerrar   (solo administrador)
// GET    /api/corte-caja
// GET    /api/corte-caja/:id

export default router
